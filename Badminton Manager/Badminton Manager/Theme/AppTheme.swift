//
//  AppTheme.swift
//  Badminton Manager
//
//  Created by Sajev Lucksman on 2026-07-04.
//

import SwiftUI

struct AppTheme {
    static let bg = Color(red: 0.06, green: 0.12, blue: 0.09)
    static let cardBg = Color.white.opacity(0.08)
    static let cardBorder = Color.white.opacity(0.10)
    static let accent = Color(red: 0.2, green: 0.9, blue: 0.6)
    static let accentSecondary = Color(red: 0.3, green: 0.8, blue: 1.0)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.6)
    static let textMuted = Color.white.opacity(0.4)
    static let courtGreen = Color(red: 0.08, green: 0.20, blue: 0.12)
    static let courtLine = Color.white.opacity(0.12)
    static let success = Color(red: 0.2, green: 0.9, blue: 0.5)
    static let warning = Color(red: 1.0, green: 0.7, blue: 0.2)
    static let danger = Color(red: 1.0, green: 0.35, blue: 0.35)
    static let purple = Color(red: 0.6, green: 0.4, blue: 1.0)
}
